<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">WebSiteName</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Accueil", authenticated_root_path %></li>
      <li><%= link_to "Nouveau Son", new_song_path %></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <% if user_signed_in? %>
            <li><%= link_to "Mon Profile", user_path(current_user) %> </li>
            <li><%= link_to "deconnexion", destroy_user_session_path, method: :delete %> </li>
        <% end %>
        </ul>
      </li>
    </ul>
  </div>
</nav>